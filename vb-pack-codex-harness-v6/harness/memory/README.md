# Memory Curation

Raw learning logs do not become active rules.

The Hermes-style loop is:

```text
observe -> cluster -> diagnose -> propose -> test -> review -> promote -> monitor
```

Use `scripts/harness/memory_guard.py audit` before promoting any repeated learning into `.agents/skills`, `AGENTS.md`, hooks, or active policy.
