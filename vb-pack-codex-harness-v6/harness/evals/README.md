# Harness Evals

Use this area for golden tasks and benchmark outputs that compare v6 overhead against earlier harness versions.

v6 fails its own intent if normal non-UI coding tasks become materially slower without catching additional real risk.
