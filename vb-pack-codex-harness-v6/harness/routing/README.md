# Routing State

Natural-language routing artifacts and route audits belong here when they are not part of the active `docs/ai/current/` work bundle.

The default active route is:

```text
docs/ai/current/Intent-Routing.json
```
