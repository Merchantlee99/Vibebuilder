# Harness Evals

Use this area for golden tasks and benchmark outputs that compare v5 overhead against earlier harness versions.

v5 fails its own intent if normal non-UI coding tasks become materially slower without catching additional real risk.
