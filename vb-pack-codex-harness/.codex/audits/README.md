# Audit Outputs

This directory stores generated audit artifacts such as insights reports and meta-audit drafts.

- These files are runtime outputs, not permanent template content.
- `bootstrap.py` and related audit scripts will recreate the directory when needed.
- Generated audit markdown is usually gitignored in downstream projects.
